__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

node_modules/
.next/
next-env.d.ts
*.tsbuildinfo

.env
.env.*
!.env.example

.DS_Store
.idea/
.vscode/

# Worktrees for the parallel tracks live next to the repo, not inside it,
# but guard against a stray one anyway.
/worktrees/

# nasiko deploy caches the agent id here. Keep it locally so a redeploy
# updates the agent rather than duplicating it, but never commit it.
.nasiko/
